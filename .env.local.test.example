# Local environment for running GitHub Actions-equivalent checks
# Copy to .env.local.test and source it:
#   cp .env.local.test.example .env.local.test
#   set -a; source .env.local.test; set +a

# For terraform workflow parity checks
TF_ENV=dev
AWS_REGION=us-east-1
# AWS_PROFILE=your-profile

# Optional: set explicit credentials instead of AWS_PROFILE
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Optional: override sensitive variable from env instead of tfvars file value
# TF_VAR_databricks_pat_value=replace-me-for-local-only
